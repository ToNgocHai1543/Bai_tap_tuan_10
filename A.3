char* a = new char[10];
char* c = a + 3;
for (int i = 0; i < 9; i++) a[i] = 'a'; 
a[9] = '\0';
cerr <<"a: " << "-" << a << "-" << endl;
cerr <<"c: " << "-" << c << "-" << endl;
delete c; // lỗi vì 'c' trỏ đến 1 vùng nhớ nằm giữa mảng được cấp phát bởi 'new char[10]' mà c++ chỉ sử dụng 'delete' cho con trỏ tỏ đến bắt đầu của mảng được cấp phát bởi new[]. sửa thành delete[] a;
cerr << "a after deleting c:" << "-" << a << "-" << endl;
